@model IEnumerable<MvcModels.Models.Library>
@{
    Layout = Request.IsAjaxRequest() ? null : "~/Views/Shared/_Layout.cshtml";
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LibraryAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LibraryName)
        </th>
        <th></th>
    </tr>

    <script type="text/javascript">
        deleteAllMarkers();

         @foreach (var item in Model) 
         {
            @:addMarker('@item.LibraryAddress', '@item.LibraryName', '@item.LibraryName');
         }
     </script>



    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LibraryAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LibraryName)
            </td>
            <td>
                @Html.ActionLink("ערוך", "Edit", new { id = item.Id }) |
                @Html.ActionLink("מחק", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>


